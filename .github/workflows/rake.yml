# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: rake

on:
  push:
    branches: [ master, main ]
    tags: [ v* ]
  pull_request:

jobs:
  notify:
    uses: metanorma/ci/.github/workflows/mn-processor-rake.yml@feature/gem-install-with-explicit-platform
    secrets:
      pat_token: ${{ secrets.METANORMA_CI_PAT_TOKEN }}
